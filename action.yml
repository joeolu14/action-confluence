name: 'My First Action'
description: 'Describe your action here.'
inputs:
  who-to-greet:
    description: 'Human being to greet.'
    default: 'World'
    required: false
outputs:
  time:
    description: 'The time we greeted you'
runs:
  using: 'node20'
  main: 'index.js'